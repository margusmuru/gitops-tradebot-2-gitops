# infrastructure

Placeholder for local-cluster definitions and operational helpers (for example a
k3d config for local bring-up, port-forward scripts, or docs). Nothing here is
consumed by ArgoCD - the GitOps loop only reads `argocd/`, `charts/`,
`environments/`, and `base/`.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Renders the in-repo common-service chart with data-service's values.yaml.
helmGlobals:
  chartHome: ../../../charts

helmCharts:
  - name: common-service
    releaseName: data-service
    valuesFile: values.yaml
